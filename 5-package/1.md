# 🟢 Go Packages

A **package** in Go is simply a **directory containing one or more Go source files**.

👉 Packages help you:

* Organize related code together
* Reuse code across projects
* Avoid naming conflicts

> Every Go program starts from the `main` package.

---

## 📁 Project Setup

### 1. Create project

```bash
mkdir go-training/package
cd go-training/package
go mod init github.com/krishnakumarkp/go-training/package
```

---

## 📦 Create a Package (`company`)

### 2. Create folder and file

```bash
mkdir company
touch company/company.go
```

---

### 3. `company/company.go`

```go
package company

import "fmt"

var companyName string = "google"

func PrintCompany() {
	fmt.Println(companyName)
}
```

👉 Notes:

* `package company` → must match folder name
* `companyName` → package-level variable
* `PrintCompany` → exported (starts with uppercase)

---

## ▶️ Use Package in `main.go`

### 4. Create `main.go`

```go
package main

import (
	"github.com/krishnakumarkp/go-training/package/company"
)

func main() {
	company.PrintCompany()
}
```

---

## 📄 Multiple Files in Same Package

### 5. Create `company/department.go`

```go
package company

import "fmt"

func PrintDepartment() {
	fmt.Printf("Engineering department at %s\n", companyName)
}
```

👉 Key Concept:

* Files in the **same package share variables/functions**
* `companyName` is accessible here

---

### 6. Update `main.go`

```go
func main() {
	company.PrintCompany()
	company.PrintDepartment()
}
```

---

## 🧠 Important Concepts

* Files in the same folder → same package
* Package-level variables are shared across files
* Export rules:

  * `PrintCompany` → ✅ accessible outside package
  * `companyName` → ❌ private (lowercase)

---

## 📂 Nested Package (`location`)

### 7. Create nested package

```bash
mkdir company/location
touch company/location/location.go
```

---

### 8. `location/location.go`

```go
package location

import "fmt"

var locationName = "Pune"

func PrintLocation() {
	fmt.Println(locationName)
}
```

---

### 9. Use in `main.go`

```go
import (
	"github.com/krishnakumarkp/go-training/package/company"
	"github.com/krishnakumarkp/go-training/package/company/location"
)

func main() {
	company.PrintCompany()
	company.PrintDepartment()
	location.PrintLocation()
}
```

---

### 10. Using nested package inside another package

```go
package company

import (
	"fmt"
	"github.com/krishnakumarkp/go-training/package/company/location"
)

func PrintCompany() {
	fmt.Println(companyName)
	location.PrintLocation()
}
```

---

## 📦 Another Package (`client`)

### 11. Create package

```bash
mkdir client
touch client/client.go
```

---

### 12. `client/client.go`

```go
package client

import "fmt"

var clientName = "Remitly"

func PrintClient() {
	fmt.Println(clientName)
}
```

---

### 13. Use in `main.go`

```go
import (
	"github.com/krishnakumarkp/go-training/package/company"
	"github.com/krishnakumarkp/go-training/package/company/location"
	"github.com/krishnakumarkp/go-training/package/client"
)

func main() {
	company.PrintCompany()
	company.PrintDepartment()
	location.PrintLocation()
	client.PrintClient()
}
```

---

## ⚠️ Import Cycle (Important)

If you modify code like:

### ❌ `client` imports `company`

```go
import "github.com/krishnakumarkp/go-training/package/company"
```

### ❌ and `company` imports `client`

👉 You will get:

```bash
import cycle not allowed
```

---

### 🧠 Why?

Go does **not allow circular dependencies**:

* `company → client`
* `client → company` ❌

---

### ✅ Best Practice

* Keep dependencies **one-directional**
* Extract shared logic into a **separate package**

---

## 🧾 Summary

* Package = directory of Go files
* Use packages to organize and reuse code
* Export using **capitalized names**
* Avoid **circular dependencies**
* Nested packages are independent packages


